package com.afterlife.aplikasikatar.repositories;

import com.afterlife.aplikasikatar.models.hakakses.TMRole;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TMRoleRepo extends JpaRepository<TMRole, Long> {
}
